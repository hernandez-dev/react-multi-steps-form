const reducer = (draft, action) => {
  switch(action.type) {
    case "set-name":
      break
    case "set-email":
      break
    case "set-phone-number":
      break
  } // switch end
}

export default reducer
